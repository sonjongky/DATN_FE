export enum ProductOrderType {
    MarketingProduct = 0,
    ProductCode = 1,
}
export type DefaultRDD = {
    AddedDaysForRDD: number;
};
export enum ProductCodeOrderType {
    Weight = 1,
    Package = 0,
}
